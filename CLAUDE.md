# Age.3 商品データベース — 引き継ぎメモ

新しいセッションで作業を頼まれたら、まずここを読むこと。
毎回ゼロから聞き直さないための共有メモ。

## これは何か

揚げサンドの商品1つ分の「商品情報シート」と「準備タスク（A-1／G-1〜G-5）」を
チームで共有するための社内ツール。作ったのは Claude（松尾さんとの作業）。

**名前は「商品データベース」。** 2026年9月に「新商品シート」から改めた。
シートは中の1画面にすぎず、実際にはタスクもビジュアルも入っているため名前が実態と
合っていなかった。ホーム画面に置くアイコンの「Age.3 DATA BASE」とも揃う。
画面に出る名前は `Sidebar.tsx` / `Header.tsx`、タブの名前は `layout.tsx` の metadata。

- 公開URL: https://age3-shinshohin-tool-theta.vercel.app/
- リポジトリ: https://github.com/klobal001-glitch/age3-shinshohin-tool
- 公開の流れ: main にコミット → Vercel が自動デプロイ（手動の作業は不要）
- データ: Supabase の共有DB（products / product_info / task_state）。
  端末ごとの保存ではなく、チーム全員が同じデータを見る。
  環境変数 NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY。
- Next.js 16 + React 19 + Tailwind v4。ローカル確認は `npm run dev`。

## 画面の構成

| ファイル | 役割 |
| --- | --- |
| `src/components/Sidebar.tsx` | 左の商品リスト（検索・ジャンル別・入力率） |
| `src/components/ProductSheetView.tsx` | 商品情報シート（入力の本体） |
| `src/components/PrepTaskView.tsx` | 準備タスク（締め切りは発売月から自動計算） |
| `src/components/VisualGalleryView.tsx` `VisualViewer.tsx` | ビジュアル一覧と全画面ビューア |
| `src/hooks/useAppData.ts` | Supabase の読み書き。入力は600ms止まったらまとめて保存 |
| `src/lib/productInfo.ts` | 入力率の数え方・価格の自動計算（Uber = 元価格 ×1.4） |
| `src/lib/prepTasks.ts` `src/lib/deadline.ts` | タスク定義と締め切りルール |

## もう1つの画面：現場チェック（/genba）

直営店（銀座・浅草・原宿）の視察を記録するための、**新商品ツールとは別のアプリ**。
同じリポジトリ・同じURLの `/genba` に置いてあるだけで、画面もデータも別。
（Claudeのログイン無しで開けるURLが必要だったため、公開済みのここに相乗りさせている）

| ファイル | 役割 |
| --- | --- |
| `src/app/genba/page.tsx` | 入口。3画面の切り替え |
| `src/components/genba/CheckView.tsx` | 現場チェック（訪問ごとに9項目・写真つき） |
| `src/components/genba/SurveyAskView.tsx` | お客様にスマホを渡して答えてもらう画面（1問ずつ） |
| `src/components/genba/SurveyView.tsx` | アンケートの集計 |
| `src/components/genba/ReportView.tsx` | 改善レポート（優先度A→B→C・印刷用） |
| `src/hooks/useGenbaData.ts` | Supabase の読み書き。使えないときはこの端末だけに保存する |
| `src/lib/genba/checkItems.ts` `survey.ts` | 10項目とアンケート6問の定義 |

- 記録の単位は「訪問（店舗＋日付）」。同じ店に期間中2回行っても別の記録として残り、
  再訪の画面には前回の指摘が出る。`genba_store` の主キーに `ginza__2026-09-02` の形で入れて
  いるので、テーブルは増やしていない（読めない行＝店舗ごとに1件だった頃の記録は引き継がない）。
- データは Supabase の `genba_store` / `genba_survey`、写真は Storage の `genba-photos`。
  **まだ作られていない場合は「この端末だけ」で動く**（作るSQLは別リポジトリ
  age3-genba-check の `supabase/schema.sql`）。新商品ツールのテーブルには触らない。
- **9項目とアンケート6問は業務に直結する。** 増減・言い換えは必ず確認を取ってから変える。
  2026年9月に松尾さんの判断で見直した：お客様インタビューはアンケートと重複するため削除、
  SNS・口コミ施策は「現場スタッフ・店長に聞く」に差し替えて最後に置いた、
  アンケートQ4は匂いから「また来たいか」に差し替え。
  **会長・専務に共有済みの出張企画資料は見直し前の内容のまま。**
- 配色は資料に合わせた紺(#1f3350)＋ティール(#2f8f9d)。新商品ツールの配色には手を入れないよう、
  この画面の色はクラスに直接書いている（globals.css は共有）。

## デザインの決めごと（`src/app/globals.css` ＋ `src/lib/ui.ts`）

2026年8月に「現行の見た目のままがよい」と決めたが、**2026年9月に2度、方針を変えた**。
1度目は「統一感を優先してよい」、2度目は松尾さんから
**「色がダサい。世の中で使われているアプリを参考に」**という指摘があり、
配色・書体・アイコンまで含めて全面的に作り直した。

**色を変えたいときは、まず `src/app/globals.css` の `@theme` を直す。**
Tailwind の stone / amber をそこで丸ごと差し替えているので、画面側のクラス名
（`text-stone-700` など）は書き換えなくても全画面に反映される。

### 何をやめて、何にしたか

| やめたもの | 今 | 理由 |
| --- | --- | --- |
| ベージュ地 `#f4ede4` | ほぼ白 `#f6f5f3`（カードは白） | 地が濁っていて、全部が古く見えていた |
| こげ茶の帯 `#4a2f1f` | 明るい面 `#fbfaf9` ＋ 細い線 | 上下が黒く縁取られて中身が窮屈だった |
| マスタードの琥珀 amber-700 | 赤みのある橙（ember）`#b84208` | 黄土色は沈む。橙のほうが「押すところ」に見える |
| Arial | OS標準（Mac は SF Pro ＋ ヒラギノ） | Arial は日本語がMS系に落ちて古く見える |
| 絵文字のタブ 🏠📝✅🖼❓ | 線のアイコン（`Icon.tsx`） | 絵文字は端末ごとに絵柄も大きさも変わる |
| 濃い枠線で区切る | 細い線＋うっすらした影 | 線が多いと画面が騒がしくなる |

- **絵文字は「中身の目印」としてだけ残す。** セクション見出しの番号＋絵文字
  （1 🧾 / 2 🥘 / 3 👩‍🍳 / 4 🖼️ / 5 📣）とタスクグループの絵文字はそのまま。
  画面まわり（タブ・ボタン・開閉の三角）には使わない。
- **アクセント（橙）は「今おすところ」にだけ。** ボタン・タブ・絞り込みの選択中だけ。
  一覧の行の選択は**薄いグレー＋左の橙の細い線**（`rowSelect`）。
  行を橙で塗りつぶすと、一覧が並んだときに画面が騒がしくなる。
- 角丸は **カード = xl ／ 押せるもの = lg ／ 丸い印 = full** の3つだけ
- 枠線は 面の区切り = stone-200 ／ 入力欄 = stone-300。濃い線は使わない
- 影は `shadow-xs`（面）／`shadow-md`・`shadow-lg`（窓・貼り付く帯）
- 文字は 本文 sm ／ 補足 xs ／ 見出し base・lg。`text-[10px]` `text-[11px]` の直書きはしない
- 見出しの太さは `font-semibold`。`font-bold` は SF Pro だと太すぎる
- 数字は `tabular-nums`（body に指定済み）。％や件数の桁がずれない
- **注記をタスク名・項目名に混ぜて書かない。** 「自動入力欄へ」のような案内を同じ大きさ・
  同じ濃さで続けて書くと、見なければいけない名前そのものが読み取りにくくなる。
  準備タスクは `TaskItem.note` に入れて、`PrepTaskView.tsx` の `TaskLabel` が
  一段小さく薄く（xs・stone-400）出す。文字列に直接足さないこと。

### `src/lib/ui.ts` にあるもの

**新しく作るときは、まずここにあるものを使う。無ければそこに足す。**
画面のファイルにクラスを直書きして、似て非なるボタンを増やさないこと。

| 使うもの | 意味 |
| --- | --- |
| `card` `cardHead` `cardBody` | 面。角丸は xl |
| `btn("primary"/"secondary"/"quiet"/"danger")` | ボタン。primary は1画面に1つ |
| `chip(active)` | 並べ替え・絞り込みの選択肢 |
| `rowSelect(active)` | 一覧の行の選択 |
| `badge("neutral"/"accent"/"good"/"warn"/"danger")` | 押せない小さな印 |
| `meter(pct)` | 細い進捗の棒。100%で緑になる |
| `field` | 入力欄（`inputCls` もこれの別名） |
| `focusRing` | フォーカスの輪郭。押せるものには必ず付ける |
| `h2` `h3` `eyebrow` `muted` | 見出し・小見出し・補足文 |
| `Icon`（`src/components/Icon.tsx`） | 画面まわりの線のアイコン |

## 運用を回すための仕掛け（2026年9月）

- **商品の切り替えは `ProductSwitcher.tsx`。** 88件をプルダウンから探すのが一番の手間だった。
  検索・最近開いた商品・右端のジャンル索引の3つで当てる。スマホは全画面、PCは中央の窓。
  入口はスマホが上の帯の商品名、PCはシート見出しの「商品を変える」。
  「最近開いた商品」は `page.tsx` で `selectedId` を見て localStorage に積んでいる。
- **左のリストのシーズンは「発売日が新しい順」だけで並べる**（2026年9月・松尾さんの指示）。
  これから発売するものも含めて日付順、古いものが下。販売終了かどうかでは分けない
  （終わった商品でも最近のものほど上にある方が探しやすい）。廃盤だけはいちばん下。
  シーズン以外のジャンルは従来どおり 販売中 → 販売終了 → 廃盤 の順。
- **ホームの先頭に「次にやること」。** 期限超過が600件あっても次の1手は分からないので、
  いちばん遅れている1件だけを大きく出して、その商品の準備タスクへ送る。
- **直近の締め切りの行は、シートではなく準備タスクへ飛ぶ。** 締め切りはタスクの話のため。
- **「今回は作らない」（2026年9月・松尾さんの確認済み）。** 作らないものが未完了のまま残ると
  遅れの数字が実態と合わないので、押して分母から外せる。**2026年9月に全タスクへ拡大した**
  （それまでは制作・入稿がある9項目だけ）。情報シートと連動するタスク（価格・レシピ画像・
  NOアルコール）だけは、シート側の入力がそのまま状態になるので対象外。
  対象外にしたものは**分母から外す**。商品ごとの設定。
  印は `task_state` に `skip|<groupId>|<milestoneId>|<taskId>` のキーで入れている
  （チェックと同じテーブル。接頭辞が違うのでぶつからない）。
  数え方は `src/lib/stats.ts` の `isTaskSkipped` を必ず通すこと。

## ビジュアル一覧の作り（2026年9月・写真アプリの作法に合わせた）

松尾さんから「Instagram や TikTok の見た目を参考に」と指示があり、
**並べ方と動かし方だけ**を写真アプリの作法に合わせた（ロゴや意匠は使っていない）。
配色は他の画面と同じ。暗い全画面・縦スワイプのフィードは入力作業に向かないので採っていない。

- **ますの一覧は正方形・隙間0.5・枠なし**（スマホ3列／PC6列）。スマホは `-mx-4` で
  本文の余白を打ち消し、画面の端まで画像を出している。カードの枠と余白をやめたのは、
  枠があると1枚あたりの画像が小さくなって「どの商品か」が絵で分からなかったため。
- 商品名はますの上に重ねる（画像があるときは黒のグラデーション＋白文字）。
  ますだけ並べるとどの商品か分からないので、写真アプリと違って名前は必ず出す。
- ますの下端の細い線がビジュアルの埋まり具合。全部そろうと緑。
- **押すと全画面ビューア（`VisualViewer.tsx`）。** 左右スワイプ／← →キーで次の商品、
  下スワイプ・Esc・外側タップで閉じる。開いている間は後ろの画面を固定している。
  それまでは画像を大きく見るのにシートを開くしかなく、見比べに商品を往復していた。
- **上の丸い列は「これから発売」**（発売日が近い順・最大14件）。
  ビジュアルが足りないものに橙の輪を付けて、先に手を付けるものが分かるようにしている。
  絞り込みの影響は受けない（いつも同じ場所にある方が押しやすい）。
- 並べ替えと絞り込みは**1列にまとめて横に流す**。2段に分けると操作盤が画面の半分を占めて、
  肝心の画像までスクロールが要った。

## レギュラー商品の準備タスク（2026年9月・松尾さんに1件ずつ確認）

レギュラー商品（レギュラースイーツ／レギュラーセイボリー＝36件）は既存の定番メニューで、
発売日が2023年のものもある。新商品の準備タスク68件が全部未チェックのまま残り、
期限超過が952件にふくらんでいた。**どれを残すかを松尾さんに1件ずつ確認して**、
残り33件を「今回は作らない」にした（`task_state` の `skip|` キー）。

**残すもの**
- A-1 開発は**全部**（撮影・材料確認・原価計算・価格・在庫・レシピ作成・最終決定・飛騨高山への公開）
- G-1 のうち：商品撮影→補正／作り方YouTube（編集・投稿・各店共有）／材料スプレッドシート／
  材料画像をもらう／Instagram フィード投稿画像・投稿文章／X・Threads の文章／
  エアレジ商品画像／Uber Eats 商品画像

**作らないもの（33件）**
- Instagram ストーリーズ画像
- ポスター・パネル7種（銀座A3・浅草A1・浅草B2・原宿A2・飛騨高山A1・嘉麻パネル・嘉麻ポスターA2）
  — **制作も入稿も丸ごと**
- 二つ折り手元メニュー・各店チラシ — 丸ごと
- レジ用ラミネートA5（関東＋飛騨高山用・嘉麻用・発送）
- ECスライダー（PC・スマホ）・サイネージ（縦・横・USB書き出し）・銀座ショーケース目隠し
- 受け渡し確認2件（事務・嘉麻スタッフ）
- G-2 PR TIMES **全10件**
- G-4 海外フランチャイズへ共有／G-5 ECスライダー整理

**発売済みのシーズン商品（15件）は、松尾さんの判断でこのまま数える。**
やり残しが見えている方がよいとのこと。勝手に外さないこと。

### ホームの「期限超過のタスク」が952件のままな理由（2026年9月に確認済み）

レギュラー商品36件を33件ずつ外しても、この数字は動かない。
**レギュラー商品はもともとこの数字に入っていない**ため。
`collectDeadlines()` は「廃盤」「継続販売中（ongoing）」「発売から1年以上たった商品」を
はじめから外しているので、2023〜2024年発売のレギュラー商品は最初から対象外だった。

952件の中身は、**発売から1年以内のシーズン／新商品（この時点で35件、うち発売済みが15件ほど）の
未チェック分**。松尾さんが「このまま数える」と決めた分なので、これは正しい数字。
減らしたいときは、その商品のタスクをチェックするか「今回は作らない」を押す。

なお「今回は作らない」を押したとき、**子項目のないタスクが数え方から抜けていない不具合**が
あった（2026年9月に修正）。`stats.ts` の `taskCompletion` / `milestoneCheckState` と
`PrepTaskView.tsx` の `isSkipped` / `milestoneProgress` の4か所すべてで、
子のあるなしを問わず `isSkipped` で `continue` すること。

## 期限超過を減らした作業（2026年9月12日・松尾さん不在の時間に実施）

952件 → **518件**。何を根拠にチェックしたかを残す。**推測でチェックは入れていない。**

### 直した数え方の不具合（2件）

1. **「今回は作らない」が子項目のないタスクで効いていなかった。**
   `stats.ts` の `taskCompletion` / `milestoneCheckState`、`PrepTaskView.tsx` の
   `isSkipped` / `milestoneProgress` の4か所すべてで、子のあるなしを問わず
   `isTaskSkipped` で `continue` すること。
2. **情報シートと連動するタスク（価格・レシピ・NOアルコール）が期限超過に残っていた。**
   商品ページ（`PrepTaskView` の `isLeafChecked`）はシートの値を見ていたのに、
   ホームの数（`stats.ts` の `milestoneCheckState`）は `task_state` のチェックだけ見ていた。
   → `milestoneCheckState` に `info` を渡して `isLinkedTaskDone` を通すようにした。
   **この2つは同じ数え方に揃えておくこと。**

### チェックを入れた根拠（この順で確認した）

| タスク | 根拠 |
|---|---|
| 商品撮影／商品撮影→補正 | 完成ビジュアルに商品写真が使われている |
| 作り方動画撮影／YouTube編集／YouTube投稿 | 情報シートに作り方動画のURLが入っている |
| 材料確認／原価計算・使用量確定／材料画像→デザイン班／材料画像をもらう／材料スプレッドシート作成 | 共有スプレッドシートに材料・分量・メーカー・原材料・内容量が入っている |
| 商品名・画像・価格・期間 最終決定 | 発売済みで価格と期間が確定している |
| Instagram フィード投稿画像 | Dropbox `1_Instagram/144ppi/` に現物がある（1枚ずつ開いて商品を確認した） |
| Instagram ストーリーズ投稿画像 | 同フォルダに縦長（9:16）の画像がある |
| 各ポスター・パネル・サイネージ・ECスライダー・レジ用A5・エアレジ画像・Uber画像の「制作」 | Dropboxの対応フォルダに現物がある |
| Instagram投稿文章／X文章／Threads文章 | 情報シートの「紹介文各種」に文章が入っている |
| G-2 PR TIMES 10件 | Dropbox `9_PRTimes/` に配信済みPDFがあり、記事URLも生きている |
| 飛騨高山にレシピを公開 | `0_国内FC事前共有/` または `12_共有情報/` に材料PDFがある |
| 新商品情報共有（G-4 海外FC） | `12_共有情報/` に JP-EN / JP-TH / JP-VI / JP-ZH の多言語一式がある |

### 「今回は作らない」にしたもの（根拠＝Dropboxに該当フォルダが無い）

Dropboxの商品フォルダが「実際に作ったものの記録」なので、フォルダが丸ごと無いものは
作っていないと判断した。**8商品すべてで共通**：二つ折り手元メニュー／各店チラシ
（シーズン商品でこのフォルダを持つものが1つもない）。

商品ごと：ティラミス＝飛騨高山ポスター・銀座ショーケース／いちじく＝飛騨高山ポスター・
銀座ショーケース・銀座ポスター／シャインマスカット＝銀座ショーケース・銀座ポスター・嘉麻ポスター／
紅蜜芋ブリュレ＝嘉麻ポスター・浅草ポスターB2／ピーナッツバタージャム＝浅草ポスターB2／
旨辛3種＝嘉麻ポスター・浅草ポスターB2。

### あえて触らなかったもの

- **入稿のチェック** … 松尾さんが押す約束。ファイルからは判断できない。
- **在庫設定／作り方YouTubeを各店の業務連絡に投稿／サイネージUSB発送／ラミネート発送／
  事務・嘉麻スタッフへの受け渡し確認** … どこにも痕跡が残らない作業なので判断材料がない。
- **レシピ作成** … ティラミス・ピーナッツバタージャム・旨辛3種はDropboxにレシピカードがある
  （`12_共有情報/.../〜recipe.jpg` や `〜レシピ.pdf`）。ただし**レシピは機密情報**と
  スプレッドシート冒頭に明記されているので、公開URLに置くのはやめた。置き場所を決めてもらう必要がある。
- **スイーツサンド／フルーツサンド6商品**（紅芋クッキークリーム・ほうじ茶クッキークリーム・
  マスカット・ゴマくちびる・芋ミイラ・いちじく10月）… 期限超過337件を占めるが、
  Dropbox `1_Age.3` 配下にも共有スプレッドシートにも資料が一切ない。別のブランド
  （fruits sand three？）の管理下と思われる。**資料の場所が分かれば同じやり方で処理できる。**
- **月見照り焼き・ヤンニョムチキン・すき焼き** … まだ準備中。Dropboxの中身も
  AIファイルや生成画像だけで、確定した現物がない。

### さらに進めた分（同日・後半）

- **情報シートと連動するタスクの数え方を揃えた**ので、価格が入っている商品の
  価格4件＋NOアルコール1件が期限超過から外れた。
- NOアルコール／NOポークは、**レジ用A5の緑のバッジ（NO ALCOHOL PORK）が根拠**になる。
  いちじく・シャインマスカットはバッジを確認して「マークを付ける」にした。
  旨辛3種はA5がPDF互換なしのAIファイルで開けないので未確定のまま。
- **バナナブリュレの素材は `過去/25_バナナブリュレ/` にある**（現行の `25_バナナブリュレ(滅)`
  には浅草A1看板しかない）。嘉麻A2看板もレシピもそちらに揃っている。
  **「フォルダが無い＝作っていない」と判断する前に `過去/` も見ること。**
- ティラミス・いちじく・シャインマスカット・紅蜜芋ブリュレ・スパイシー3種・
  ピーナッツバタージャムには `過去/` フォルダは無い（＝今回の判断はそのままでよい）。

**結果：952件 → 513件。**残りの内訳は
スイーツ／フルーツサンド6商品 336件（資料が見つからない）／月見照り焼き 40件（準備中）／
発売済み8商品 約14件ずつ（ほぼ入稿と痕跡の残らない作業）／
ヤンニョムチキン2件・すき焼き 各7件（準備中）／バナナブリュレ 2件。

## 共有スプレッドシートとの関係（2026年9月12日）

「シーズン揚げサンド(Seasonal)直営共有」
`https://docs.google.com/spreadsheets/d/10zq0KppX6hLUhobz8YCKClcTRQdTjDwETEpV6AjjQWw/`
が、国内外FCに渡す正式な資料。**旨辛チーズカレーのタブが基準の形**。

- **40タブすべてに【注意事項】（日英）を入れた。** 機密であることと、販売希望時の
  手順（試作の写真・動画を日本チームへ／日本チームの確認・合意のうえで発売）。
- いちじく・シャインマスカットのタブは基準の形に合わせて作り直した。
  情報解禁日・日本発売開始日、ビジュアル欄（嘉麻パネル・ECスライダーPC/スマホの行とサイズ）、
  紹介文各種（Instagramコメント／取引先・関係者一斉メール／X文章／Threads文章／
  PRTimes記事URL／ストーリーズに貼る文章の日英）。
- 旨辛ナポリタン・旨辛焼肉にも紹介文各種を追加。**旨辛3種は1本のまとめ文章**なので、
  先頭に「※旨辛3種まとめての文章です」と入れて3商品に同じ文を入れてある。
- **ビジュアル欄の画像は `=IMAGE("https://age3-shinshohin-tool-theta.vercel.app/visuals/○○.jpg",1)`**
  で表示している。Dropboxの144ppi版を長辺1200pxに縮小してツール側に置いたもの。
  初回だけ「外部との通信を許可」を押す必要がある。
  ファイル名は `fig_*` / `muscat_*`（ig_ja, ig_en, poster, signage_v, signage_h, panel, ec_pc, ec_sp）。
- **シャインマスカットのタブにミックスベリーオペラの画像が4枚入っていた**ので削除した。
  他のタブにも別商品の画像が残っている可能性がある。

### 情報解禁日の調べ方

PR TIMESの記事ページを開くと配信日時が出る。それが情報解禁日。確認済み：
ティラミス 2026/06/25（記事93）／ピーナッツバタージャム 2026/06/24（記事96）／
旨辛3種 2026/07/27（記事105）／秋季3品（紅蜜芋ブリュレ・いちじく・シャインマスカット）
2026/08/26（記事106）／アイスクリームブリュレ 2026/05/26（記事89）。

⚠️ **ピーナッツバタージャムのPR TIMES URLが記事89（アイスクリームブリュレ）になっていた**ので
記事96に直した。紅蜜芋ブリュレのタブにも記事105（旨辛3種）が入っていた。**URLは記事を開いて確認すること。**

### スプレッドシートを機械で編集するときの注意

- 読むのは `gviz/tq?tqx=out:csv&headers=0&sheet=<タブ名>`（`/export?format=csv` は失敗する）。
  **空行が落ちるので、行番号は画面で確認すること。**
- 書くのは「クリップボードにTSVを入れて Cmd+V」。セル内改行を残すには
  `"1行目\n2行目"` のようにダブルクォートで囲む。
- **貼り付ける直前に必ずクリップボードを入れ直す。** 行の挿入などをすると
  Googleスプレッドシート内部のクリップボードが優先されて、意図しない内容が貼られる。
  Escapeを1回押してから貼ると安定する。
- タブの移動は Option+↓ / Option+↑（クリックは座標がずれて失敗しやすい）。
- 行の選択は名前ボックスに `1:2` と入れる（`A1:A4` は結合セルで範囲が広がる）。
- **結合セルを含む行に行を挿入すると内容が複製されることがある。** 挿入後に必ず確認する。
  （アイスチョコいちごのタブで発生。松尾さんのメモが複製されたので消した）

## ビジュアルの「ありません」（2026年9月・松尾さんの指示）

商品によっては、そのビジュアルが**そもそも存在しない**。レギュラー商品は
Instagram投稿もポスターもサイネージも作らず、あるのは
**商品画像（背景なし）・Uber画像・エアレジ画像の3つだけ**。
未入力のまま残ると進捗が実態と合わないので、枠ごとに押して分母から外せるようにした。

- 印は `product_info.data.visualDownloads[].na`（商品ごと・枠ごと）。
  **true = ありません（分母から外す）／false = あります（必須に数える）／未設定 = ジャンルの既定**
- 数え方は `src/lib/productInfo.ts` の `isRequiredVisualKey(genre, key, group)` を必ず通すこと。
  `requiredVisualTotal` は第2引数に `info` を渡すと「ありません」を反映した数を返す。
- ジャンルの既定は従来どおり（`REGULAR_REQUIRED_VISUAL_KEYS`）。レギュラー商品は3件だけ必須。
  `na` が入っていればそちらが優先されるので、**例外を作れる**のがこの仕組みの要点。
- **例外：バナナブリュレ。** シーズンからレギュラー化した商品で、各ビジュアルが揃っている。
  全枠に `na: false` を入れて、ジャンルの既定を無視して全件を必須に数えている。
- `normalizeVisualGroups` は保存のたびに枠を作り直すので、**`na` を必ず引き継ぐこと**。
  落とすと設定が毎回消える。

## ビジュアルのURL（2026年9月に整理）

- **リンクは相対パスで持つ。** ツール自身に置いた画像は `/visuals/○○.jpg` の形。
  `https://age3-shinshohin-tool.vercel.app/...` のような絶対URLは書かないこと。
  **このドメインは生きていない**（公開URLは `-theta` が付くほう）。以前このURLで29件登録されていて、
  全部リンク切れになっていた。ドメインが変わっても切れないよう、相対パスに統一した。
- **Instagram フィード画像27件は `public/visuals/` に置いてある。** Dropboxから元画像を探し、
  1枚ずつ中身を確認して 1080×1350 に縮小したもの。Dropboxリンクと違って読み込みが速く、
  「⬇ 保存」も確実に効く（同じサーバー上のファイルなので）。
  入稿用の大きいデータ（ポスター・サイネージ・パネル）は従来どおりDropboxリンクのまま。
- **はみ出る鰻玉だけ Instagram 画像が存在しない**（Dropboxにも無い）。リンクは空にしてある。
- Dropboxの元画像は `<商品フォルダ>/<年>/1_Instagram/144ppi/` にある。
  `1_Instagram` 直下は .ai（イラレ）なので、JPGは必ず `144ppi` の中を見る。
  古い商品は `〜_Instagram_J.jpg`（J＝日本語版・E＝英語版）という名前のこともある。
  シリーズ物（アイス揚げサンド・スパイシーシリーズ）は1つのカルーセルに複数商品が入っている。

## 商品名の表記（勝手に直さないこと）

「商品名マスタ（2026年10月1日適用・全店改変）」に合わせている。2026年9月に反映済み。

- **産地名は品名の前に置く。** 「シナモンアップルパイ（青森県）」ではなく
  「青森県 シナモンアップルパイ」。間は全角スペース1つ。
  該当：青森県 シナモンアップルパイ／石川県 旨だれ牛カルビ焼肉／宮崎県 チキン南蛮／
  北海道 炙りチーズサーモン（スモークサーモン）
- 産地ではない補足（原料の種類・店舗限定など）は、従来どおり品名のうしろに括弧で付ける。
  例：（スモークサーモン）（茅ヶ崎限定）
- 商品名は `products.name`（リストの表示名）と `product_info.data.nameJa`（シートの品名）の
  2か所にある。**片方だけ直すとリストとシートで食い違う。** 必ず両方を揃える。
- 英語名も同じマスタが正。Brûlée／Crème／Crêpe はアクセント付き。
  日本の料理名（Tsukimi／Namban／Yakiniku／Mentai）を含む英語名は使わない。
- マスタの「再販時に修正」欄の商品は、10月1日の変更対象外。再販のタイミングまで現状のまま。

## スマホでの見え方（2026年9月に調整）

配布前にスマホで一通り触れるようにした。次に直すときも、以下は戻さないこと。

- **画面の切り替えタブは下端に固定**（`Header.tsx`）。片手で持ったときに親指が届くのは
  画面の下半分なので、いちばん押す5つを下に置いている。PCは従来どおり左のサイドバー。
  選んでいるタブは橙の線アイコン＋橙の文字（iPhoneの標準アプリと同じ見せ方）。
  本文が隠れないよう `page.tsx` の main に `pb-28 md:pb-6` を入れてある。
- **入力欄の文字はスマホだけ16px**（`globals.css`）。iOS Safari は16px未満の入力欄を
  タップすると画面を勝手に拡大する。14pxのままだと入力のたびに拡大され、横スクロールが要る。
- **材料の表は、スマホでは1行＝1枚のカードに積み替える**（`globals.css` の `.ing-table`）。
  ラベルは `<td>` の `data-label` から出している。表の操作（Tabで右・最後の行でEnterで追加）
  はそのまま。
- **シート内のタブ・並べ替えの列は折り返さず横に流す**（`.scroll-x-clean`）。
  折り返すと貼り付いた見出しが画面の半分を占めて、肝心の入力欄が見えなくなる。
  今いるセクションのタブは自動で見える位置まで寄る。
- **指で押すものは高さ44px以上**（`min-h-11`）。PCでは `md:min-h-0` で元の高さに戻す。
- 商品の「追加・改名・廃盤・削除」は、スマホでは「商品の管理」に畳んである
  （`ProductPicker.tsx`）。日々の入力では使わないうえ、押し間違いが起きやすいため。

## 入力まわりの決めごと

- 材料は表。`Tab` で右のセル、最後の行で `Enter` を押すと行が増える
- ビジュアルのリンクは、枠にURLを貼って `Enter` で登録
- 必須項目の左の点は 未入力＝赤／入力済み＝緑
- 「次の未入力へ」で空いている必須項目にジャンプ、「未入力だけ表示」で絞り込み
- 右上に「保存中…→ 保存しました」を出す（`useAppData` の `saveState`）

## 作業のしかた

- 松尾さんは iMac と MacBook を使い分ける。作業はクラウド側で完結するので、
  どちらのMacかは気にしなくてよい。ローカルのファイルを触りに行く必要はない。
- 変更は必ず `npm run build` と `npx eslint` を通してからコミットする。
- 修正は main に直接コミット・プッシュする。ブランチを切ってそのままにしない。
  プルリクエストは作らない（松尾さん1人の運用のため）。
- 「公開まで済ませる」が既定。main に入った時点で Vercel が自動公開する。
  作業の最後に、公開URLに反映される見込みかどうかを一言添える。
- 締め切りの計算ルール・項目の増減は業務に直結する。勝手に変えず、必ず確認する。


